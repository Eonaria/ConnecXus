const mysql = require('mysql2/promise'); async function run() { const conn = await mysql.createConnection({host:'localhost', user:'root', password:'', database:'connecxus_db'}); try { const [rows] = await conn.query('SELECT c.id, c.name, c.slug, c.description, c.created_at, c.is_suspended, u.username as owner_username, u.display_name as owner_name, (SELECT COUNT(*) FROM community_members WHERE community_id = c.id AND status = \'active\') as member_count FROM communities c LEFT JOIN users u ON c.owner_id = u.id ORDER BY c.created_at DESC'); console.log(rows); } catch(e) { console.error(e); } process.exit(0); } run();
